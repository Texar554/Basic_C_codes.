#include<stdio.h>
int main()
{
    int n1, n2, sum;
    int add (int a, int b);
    printf("\n Enter two number-->");
    scanf("%d %d",&n1, &n2);
    sum=add(n1,n2);
    printf("Sum-->%d",sum);
    return 0;
}

int add(int a, int b)
{
    int r;
    r=a+b;
    return r;
}
